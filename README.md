# Citadel
Sets up all the `homeshick` castles for my dotfiles. This includes:

* `zsh`
* `neovim`
* `tmux`

## Dependencies
 * `git`
 * `zsh`
 * `curl`
 
For `python`/`pyenv`
 * `readline`
 * `zlib`
 * `bz2`
 * `sqlite3`
 * `openssl`
 
For `zplug`
 * `gawk`
